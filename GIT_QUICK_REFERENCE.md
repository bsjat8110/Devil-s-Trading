# 🚀 Git Quick Reference

## Daily Commands
```bash
# Check status
git status

# Add all changes
git add .

# Commit
git commit -m "Message"

# Push
git push origin main

# Pull latest
git pull origin main

